{
  "runcontract": 1,
  "services": [
    {
      "name": "validkit",
      "dir": ".",
      "stack": "python",
      "kind": "cli",
      "install": [
        ["${python}", "-m", "pip", "install", "-q", "-e", "."]
      ],
      "start": [
        "${python}",
        "-c",
        "import validkit; print(sorted(validkit.__all__)); assert validkit.is_valid_email('alice@example.com') is True; assert validkit.is_valid_email('') is False; assert validkit.is_valid_email('alice@') is False; assert validkit.is_valid_email('@example.com') is False; assert validkit.is_valid_email('aliceexample.com') is False; print('validkit ok')"
      ]
    }
  ]
}
